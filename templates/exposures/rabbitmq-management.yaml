id: rabbitmq-mgmt
name: "RabbitMQ Management Exposed"
severity: high
description: "RabbitMQ management interface is publicly accessible."
reference: CWE-284
requests:
  - method: GET
    path: "/api/overview"
    matchers:
      - type: body
        words:
          - "rabbitmq_version"
      - type: status
        status: [200]
    condition: and
